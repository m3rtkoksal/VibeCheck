import SwiftUI

/// Dashboard placeholder for self-profile insight.
/// The project file referenced this source but it was missing.
struct SelfProfileInsightDashboardView: View {
    var body: some View {
        ProfileCharacterInsightView()
    }
}

#Preview {
    NavigationStack {
        SelfProfileInsightDashboardView()
    }
}

